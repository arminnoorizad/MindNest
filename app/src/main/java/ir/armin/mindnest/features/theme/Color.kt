package ir.armin.mindnest.features.theme

import androidx.compose.ui.graphics.Color

// Primary / Accent
val LightPurple = Color(0xFFE8DEF8)
val LightPurpleDark = Color(0xFFB8A0D9)

// Dark charcoal for bottom bar (zaghali)
val DarkCharcoal = Color(0xFF232526)

// Icon dark on light purple button
val IconDark = Color(0xFF49454F)


val Primary = LightPurpleDark
val OnPrimary = Color.White

val Surface = Color(0xFFFFFFFF)
val OnSurface = Color(0xFF1C1B1F)

val Background = Color(0xFFF7F6FB)
val OnBackground = Color(0xFF1C1B1F)

val TopBarColor = Color(0xFFE8ECFF)
val FabContainer = LightPurple
val FabContent = LightPurpleDark
val DeleteRed = Color(0xFFD32F2F)
